# Environment variables
# Copy this file to .env.local and fill in the values

# API endpoints
VITE_API_BASE_URL=https://api.example.com

# Public keys (safe to expose in frontend)
VITE_PUBLIC_API_KEY=your_public_api_key

# Feature flags
VITE_ENABLE_ANALYTICS=false
VITE_ENABLE_DEBUG=false

# Vercel Environment Variables (set in Vercel dashboard)
# These are used by the serverless functions, not exposed to frontend
# TAGGER_CSRF_TOKEN=your-csrf-token
# TAGGER_SESSION_COOKIE=your-session-cookie